{% extends "::adminLayout/layout.html.twig" %}

{% block body %}
    <div id="main">

    <!--        Caroussel      -->
    <div id="myCarousel" class="carousel slide col-xs-12 col-sm-12 col-md-12 col-lg-12 carousel-fade" data-ride="carousel">
        <!-- Indicators -->
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            {% for image in images %}
                <li data-target="#myCarousel" data-slide-to="{{ image.ordre }}"></li>
            {% endfor %}
        </ol>

        <!-- Wrapper for slides -->
        <div class="carousel-inner centrageD">
            <div class="item active" style="width: 100%;">
                <img class="ImageCarousel d-block img-fluid" src="{{  asset(active.media.path) }}" alt="Los Angeles">
                <div class="carousel-caption TextNewCaroussel">
                    <h3 class="animated fadeInLeft {{ active.placement }} hidden-xs col-lg-5">
                            <span class="ColorationText">
                                {{ active.text }}
                            </span>
                    </h3>
                </div>
            </div>
            {% for image in images %}
                <div class="item" style="width: 100%;">
                    <img class="ImageCarousel d-block img-fluid" src="{{ asset(image.media.path) }}" alt="Los Angeles">
                    <div class="carousel-caption TextNewCaroussel">

                        <h3 class="animated fadeInRight {{ image.placement }} hidden-xs col-lg-5">
                                <span class="ColorationText">
                                    {{ image.text }}
                                </span>

                        </h3>
                    </div>
                </div>

            {% endfor %}
        </div>
        <!-- Left and right controls -->
        <a class="left carousel-control" href="#myCarousel" data-slide="prev" style="background: none;">
            <span class="glyphicon glyphicon-chevron-left"><i class="fa fa-angle-double-left" aria-hidden="true"></i></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="right carousel-control" href="#myCarousel" data-slide="next" style="background: none;">
            <span class="glyphicon glyphicon-chevron-right"><i class="fa fa-angle-double-right" aria-hidden="true"></i></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
    <!--        !Caroussel!      -->


{% if not image.media is null%}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12">
        <div>
            <img class="ImageCarouselAdmin col-xs-6 col-sm-6 col-md-6 col-lg-6" src="{{ asset(image.media.path) }}">
        </div>
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% else %}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12" >
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form) }}
            {{ form_end(form) }}
        </div>
    </div>
    {% endif %}


{% if not image1.media is null%}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <img class="ImageCarouselAdmin col-xs-6 col-sm-6 col-md-6 col-lg-6" src=" {{ asset(image1.media.path) }}">
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form1) }}
            {{ form_end(form1) }}
        </div>
    </div>
{% else %}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12" >
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form1) }}
            {{ form_end(form1) }}
        </div>
    </div>
    {% endif %}


{% if not image2.media is null%}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <img class="ImageCarouselAdmin col-xs-6 col-sm-6 col-md-6 col-lg-6" src="{{ asset(image2.media.path) }}">
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form2) }}
            {{ form_end(form2) }}
        </div>
    </div>
{% else %}
    <div class=" DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12" >
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form2) }}
            {{ form_end(form2) }}
        </div>
    </div>
    {% endif %}


{% if not image3.media is null%}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <img class="ImageCarouselAdmin col-xs-6 col-sm-6 col-md-6 col-lg-6" src="{{ asset(image3.media.path) }}">
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form3) }}
            {{ form_end(form3) }}
        </div>
    </div>
{% else %}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12" >
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form3) }}
            {{ form_end(form3) }}
        </div>
    </div>
{% endif %}


{% if not image4.media is null%}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12">
    <img class="ImageCarouselAdmin col-xs-6 col-sm-6 col-md-6 col-lg-6" src="{{ asset(image4.media.path) }}">
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form4) }}
            {{ form_end(form4) }}
        </div>
    </div>
{% else %}
    <div class="DivCarouselAdmin col-xs-12 col-sm-12 col-md-12 col-lg-12" >
        <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6">
            {{ form_start(form4) }}
            {{ form_end(form4) }}
        </div>
    </div>
    {% endif %}

{% endblock %}