{% extends "::layout/layout.html.twig" %}
{% block body %}

    {% for lieu in lieux %}
        <section id="IrakLieu" class="col-xs-12 col-sm-12 col-md-12 col-lg-9 ">
            <h3 id="TitreLieu" class="col-xs-4 col-sm-4 col-md-4 col-lg-4"> {{ lieu.nom }} ? </h3>
            {#{% if origine.media is not null %}#}
                {#<img id="ImgOrigine" class="col-xs-4 col-sm-4 col-md-4 col-lg-4" src="{{ asset(origine.media.path) }}">#}
            {#{% endif %}#}
            <div id="DescriptionLieu" class="col-xs-5 col-sm-5 col-md-5 col-lg-offset-1 col-lg-5">
                <p>{{ lieu.description }}</p>
            </div>
        </section>
    {% endfor %}
{% endblock %}